apiVersion: apps/v1
kind: Deployment
metadata:
  name: backend-config-server-deployment
  labels:
    app: backend-config-server-deployment
spec:
  selector:
    matchLabels:
      app: backend-config-server-deployment
  replicas: 1
  template:
    metadata:
      labels:
        app: backend-config-server-deployment
    spec:
      containers:
        - name: backend-config-server
          image: cosiobit/backend-config-server
          ports:
            - containerPort: 8888
---
apiVersion: v1
kind: Service
metadata:
  name: backend-config-server-service
  labels:
    app: backend-config-server-service
spec:
  selector:
    app: backend-config-server-deployment
  ports:
    - protocol: TCP
      port: 8888
      targetPort: 8888
  type: LoadBalancer