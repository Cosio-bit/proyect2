apiVersion: v1
kind: PersistentVolume
metadata:
  name: marca-db-pv
  labels:
    type: local
spec:
  storageClassName: standard
  capacity:
    storage: 1Gi
  accessModes:
    - ReadWriteOnce
  hostPath:
    path: "/data/marca-db-data"
---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: marca-db-pvc
  labels:
    app: postgres
    tier: database
spec:
  storageClassName: standard
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: marca-db-deployment
  labels:
    app: marca-db-deployment
    tier: database
spec:
  selector:
    matchLabels:
      app: marca-db-deployment
  strategy:
    type: Recreate
  template:
    metadata:
      labels:
        app: marca-db-deployment
        tier: database
    spec:
      containers:
        - name: marca-db
          image: postgres
          imagePullPolicy: "IfNotPresent"
          ports:
            - containerPort: 5432
          env:
            - name: POSTGRES_USER
              valueFrom:
                secretKeyRef:
                  name: postgres-credentials
                  key: DB_USER_ORDERS
            - name: POSTGRES_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: postgres-credentials
                  key: DB_PASSWORD_ORDERS
          volumeMounts:
            - mountPath: /var/lib/postgresql/data
              name:  marca-data
      volumes:
        - name: marca-db-data
          persistentVolumeClaim:
            claimName: marca-db-pvc
---
apiVersion: v1
kind: Service
metadata:
  name: marca-db-service
  labels:
    app: marca-db-service
    tier: database
spec:
  ports:
    - port: 5432
      targetPort: 5432
      protocol: TCP
  selector:
    app: marca-db-deployment
    tier: database
  type: LoadBalancer
