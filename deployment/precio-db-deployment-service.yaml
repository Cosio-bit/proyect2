apiVersion: v1
kind: PersistentVolume
metadata:
  name: precio-db-pv
  labels:
    type: local
spec:
  storageClassName: standard
  capacity:
    storage: 1Gi
  accessModes:
    - ReadWriteOnce
  hostPath:
    path: "/data/precio-db-data"
---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: precio-db-pvc
  labels:
    app: postgres
    tier: database
spec:
  storageClassName: standard
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: precio-db-deployment
  labels:
    app: precio-db-deployment
    tier: database
spec:
  selector:
    matchLabels:
      app: precio-db-deployment
  strategy:
    type: Recreate
  template:
    metadata:
      labels:
        app: precio-db-deployment
        tier: database
    spec:
      containers:
        - name: precio-db
          image: postgres
          imagePullPolicy: "IfNotPresent"
          ports:
            - containerPort: 5432
          env:
            - name: POSTGRES_USER
              valueFrom:
                secretKeyRef:
                  name: postgres-credentials
                  key: DB_USER_MARCA
            - name: POSTGRES_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: postgres-credentials
                  key: DB_PASSWORD_MARCA
          volumeMounts:
            - mountPath: /var/lib/postgresql/data
              name:  precio-data
      volumes:
        - name: precio-db-data
          persistentVolumeClaim:
            claimName: precio-db-pvc
---
apiVersion: v1
kind: Service
metadata:
  name: precio-db-service
  labels:
    app: precio-db-service
    tier: database
spec:
  ports:
    - port: 5433
      targetPort: 5432
      protocol: TCP
  selector:
    app: precio-db-deployment
    tier: database
  type: LoadBalancer
  